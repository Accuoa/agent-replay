# Roadmap

## Done
- v0.1 alpha: minimum demoable path — recorder, replayer, toy agent, CLI, 30-fixture benchmark, Strong-band calibration.

## In flight
- Documentation polish.

## Planned
- v0.2: optional `metadata.attestation` envelope (Plan 8 composition).
- v0.2: optional `replayCheckpoint(name, value)` boundary for controller-internal hashing.
- v0.2: recommended boundary naming convention as informative text in SPEC.

## Maybe
- Streaming / partial-response replay.
- Compatibility with OpenAI Assistants API trace format.
- Branching / non-linear traces.
